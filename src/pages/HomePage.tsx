export default function HomePage() {
  return (
    <div className="min-h-screen bg-white p-8">
      <h1 className="text-2xl font-semibold text-gray-900">Triple Report</h1>
      <p className="mt-2 text-gray-600">
        Build new pages from Figma: select a frame in Figma and ask the agent to generate the page using the Figma MCP.
      </p>
    </div>
  )
}
